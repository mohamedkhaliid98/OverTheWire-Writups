# Bandit Level 10 → 11

## Objective
The password is stored in `data.txt` as Base64 encoded data.

## Tools Used
- `base64`

## Process
I decoded the file directly:

`base64 -d data.txt`

## What I Learned
- Identifying and decoding Base64 strings.
